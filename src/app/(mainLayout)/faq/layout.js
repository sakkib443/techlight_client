export const metadata = {
    title: "FAQ",
    description: "Frequently Asked Questions about Techlight IT Institute — enrollment, payment, certificates, courses and technical support.",
};

export default function FAQLayout({ children }) {
    return children;
}
